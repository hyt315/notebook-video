// 主题开关（LOCKED）：本文件只允许改写 import 来源路径，由 new-project --style 写入。
// 可选值：./paper（默认暖米纸） ./cel ./sticker ./flat
// 注意：必须保持「先 import 再 export」两行形式，单行再导出在打包器下会失效。
import {THEME} from './paper';
export {THEME};
